Renomear a coluna "Followup" para "Oportunidade futura" no funil de Contas.

Alteração única em `src/lib/contasFunil.ts` linha 24: label `"Followup"` → `"Oportunidade futura"` (id `perdido` permanece intacto para não quebrar dados existentes).